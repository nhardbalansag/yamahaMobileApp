class Products {
    constructor (
        id, 
        photo_path, 
        title,
        description,
        status,
        update_count,
        price,
        product_category_id
        ){
            this.id = id;
            this.photo_path = photo_path;
            this.title = photo_path;
            this.description = photo_path;
            this.status = photo_path;
            this.update_count = photo_path;
            this.price = photo_path;
            this.product_category_id = photo_path;
    }
}

export default Products;
